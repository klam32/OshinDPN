# Frontend uses the same-origin /api proxy. No frontend API keys are needed.
# Copy backend/.env.example to backend/.env for Gemini and SMTP configuration.
# Never put secret API keys in VITE_* variables.
